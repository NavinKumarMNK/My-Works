[project]
name = "django-saas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.2",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
]
